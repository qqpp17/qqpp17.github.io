# 2026-08-13

## 添加个人简历导航页面（14:00）

**需求**：在个人网站上搭建个人简历导航

**实现方式**：利用 dream 主题的翻转页面（flip）+ 卡片墙（masonry）机制
- 创建 `content/about/` 目录，内含 `index.md`（headless: true）+ 5 张简历卡片
- 卡片内容：个人简介(me.md)、教育背景(education.md)、专业技能(skills.md)、项目经验(projects.md)、联系方式(contact.md)
- 导航栏自动显示"关于"按钮（dream 主题内置 about 导航项），点击触发页面翻转，背面以卡片墙形式展示简历
- hugo.toml 添加 `reorderNavItems = ["about", "posts", "categories", "tags"]` 控制导航顺序
- welcome.md 更新：PaperMod → Dream，添加简历页说明

**部署**：GitHub Actions Run 5 成功，线上验证 dream-grid-about + 5 个 card-title 全部渲染正常

**关于主题切换 CSS**：发现 dream 主题 src/input.css 中 `themes: emerald --default, forest --prefersdark` 只包含 2 个主题，但 hugo.toml 配置的是 autumn/synthwave。已修改 input.css 并用 `npx tailwindcss` 重新构建 output.css。用户可能已自行提交修复（commit deae0e6）。

**注意**：dream 主题没有内置前端语言切换按钮。Hugo 多语言是站点级配置，不是运行时按钮切换。
