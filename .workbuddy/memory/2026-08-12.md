# 2026-08-12

## 个人博客 (Hugo + PaperMod) 搭建/运行排查

**现象**：用户无法把站点跑起来访问 https://qqpp17.github.io/

**根因 1（部署阻断，需用户在 GitHub 操作）**：
- 仓库 remote = `git@github.com:qqpp17/qppp17.github.io.git`
- GitHub 账号(owner) = `qqpp17`，但仓库名 = `qppp17.github.io`（少一个 q）
- 用户页要显示在 `https://qqpp17.github.io/`，仓库必须命名为 `qqpp17.github.io`（与账号名完全一致）
- 修复：GitHub 仓库 Settings → Repository name 改为 `qqpp17.github.io`；Pages Source 设为 "GitHub Actions"

**根因 2（本地构建报错，已修复）**：
- `hugo` 报 `open .../public/tags/index.html: Access is denied`
- 原因：public/ 目录有被锁定的旧构建产物。清理 public/ 后构建正常（HTTP 200）

**所做修改**：
- hugo.toml：去掉废弃的 `languageCode`，改用 `defaultContentLanguage` + `[languages.zh-cn]`（locale/label），消除配置告警
- 新增 content/posts/welcome.md 示例文章（content/ 原本为空，站点无内容）
- 本地 `git commit`（main f3fad41），未 push

**环境**：Hugo v0.164.0 extended 已装；gh CLI 不可用；PaperMod 主题直接提交在 themes/PaperMod（无 .gitmodules 子模块）

## 部署成功 + 换 Dream 主题（下午 16:00-17:00）

**部署最终成功**：
- GitHub Pages 返回 HTTP 200。但页面标题显示 "deployed | qqpp17"（README 默认页），说明 GitHub 自带 workflow 仍可能覆盖 Actions 部署
- 关键点：GitHub Pages Settings → Source 必须 = GitHub Actions；若被自带 workflow 覆盖，需 Unpublish site 后重新部署
- 部署内容与 README 冲突：README.md 会被 Jekyll 渲染成默认页，若想避免干扰可删 README 或确保 Actions 部署生效

**换主题（PaperMod → hugo-theme-dream）**：
- 用户选择"卡片墙风格"（hugo-theme-dream）+ 换主题时一起处理部署
- 主题下载方式：git clone 失败（网络连接重置），改用 codeload.github.com 下载 zip 包（curl），Python zipfile 解压
- 主题目录：themes/hugo-theme-dream/，运行文件直接提交（layouts/assets/i18n/archetypes），docs/src/decapCMS 等构建源码加入 .gitignore
- hugo.toml 配置：theme = "hugo-theme-dream"，lightTheme = "autumn"（秋日暖色），darkTheme = "synthwave"（霓虹炫酷），stickyNav/showTableOfContents/showPrevNextPost 等
- 注意：dream 主题 theme.toml 的 name 是 "Dream"，但 hugo.toml 里 theme 填目录名 "hugo-theme-dream"
- 本地构建 13 页正常，卡片墙（dream-grid/masonry）+ 默认封面图（default1-4.jpg）渲染正常

**遗留问题**：
- themes/hugo-theme-dream-master/ 残留目录无法删除（safe-delete 拦截），已加入 .gitignore
- ~~需确认部署后显示的是 Hugo 内容而非 README 页~~ ✅ 已确认：网站显示 Dream 主题（dream-grid/masonry 卡片墙、autumn/synthwave 主题色、default1-4.jpg 封面），HTTP 200，标题"潘华珍的博客"

**最终结果（17:00 左右）**：新主题部署成功上线 https://qqpp17.github.io，卡片墙风格 + 多彩主题色（亮色 autumn / 暗色 synthwave）
